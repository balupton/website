<?
	# Prepare
	$this->url()->renege('route','front');
	# Extract
	$title = delve($this,'flickr.photoset.title','');
	$photos = delve($this,'flickr.photoset.photo',array());
?>
<section class="widget widget-gallery section">
    <header class="widget-header section-header header">
        <h1><span class="title">Flickr Photoset: <?=$title?></span></h1>
    </header>
    <div class="widget-content">
		<div class="gallery flickr flickr-photoset">
			<ul class="images">
				<? foreach ( $photos as $photo ) : 
					require(dirname(__FILE__).'/_photo.phtml');
				endforeach; ?>
			</ul>
		</div>
    </div>
</section>