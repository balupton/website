<?
	# Prepare
	$this->url()->renege('route','front');
	# Extract
	$search = delve($this,'search');
	$ContentList = delve($this,'ContentList',array());
	# Adjust
	$title = 'Search: '.delve($search,'query','N/A');
?>
<?= $this->content()->renderContentlistWidget(array('title'=>$title,'ContentList'=>$ContentList)) ?>